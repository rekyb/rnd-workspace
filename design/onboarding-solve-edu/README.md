# Learner Acquisition & Onboarding

**Status:** Active
**Started:** 2026-07-21
**Informed by:** research/2026-07-20-unified-onboarding-synthesis-and-patterns (litreview, peer-reviewed 2026-07-20; coverage Q1,Q3 answered · Q2 partial), research/2026-07-28-post-signup-handoff-first-run-home (benchmark, peer-reviewed 2026-07-29; coverage Q2,Q3,Q5 answered · Q1,Q4 partial · Q6 unanswered), research/2026-07-29-learning-home-layout-and-ia (benchmark, peer-reviewed 2026-07-29; coverage Q1,Q5 answered · Q2,Q3,Q4 partial · Q6 withdrawn)
**Design system:** independent — 23 tokens hand-copied into `styles.css`. **Settled 2026-07-28: this project will not migrate to `ui-library/`.** Measured cost of migrating: 46 of its 49 classes are absent from `components.css` (gate rule 4 has no project-class exemption), `styles.css` carries 224 raw px and 22 raw hex values (rule 2), and 10 of its 23 token names do not exist upstream — which an overlay may not introduce. That is a rebuild of a working, tested prototype, not a migration. See `.claude/references/design-projects.md`.

## Problem

Learners arrive at the platform with very different amounts of context — some with a
facilitator-issued program code and a destination already decided, some browsing with no
idea what to learn, some returning to an account they already have. One undifferentiated
entry path forces all three through decisions that only apply to one of them, and loses
program attribution along the way. This project defines the single acquisition and
onboarding funnel that serves all three without that cost.

## Files

- `PRD.md` — the decision doc (Shape-Up format, 17 sections + the Prototype Element Dictionary + `## Stakeholder Review`).
- `prototype-web.html` + `styles.css` + `main.js` + `data.js` — the multi-file prototype
  source. Predates the `src/` convention in `.claude/references/design-projects.md`.
- `build-standalone.ps1` — inlines the above into `standalone.html` (gitignored,
  ~3 MB), base64-encoding `img/` and failing loudly on a missing asset.
- `prototype-web.test.ps1` — the flat reference prototype's regression suite (12 groups).
- `src/` — the current build: `onboarding.html` + `home.html` + `main.js` + `home.js` + `data.js`
  + `styles.css` + `img/`, following the `src/` convention in
  `.claude/references/design-projects.md`.
- `src/src-prototype.test.ps1` — the `src/` build's regression suite (83 checks, 15 groups). It
  exists because the flat prototype rendered a 1-day streak, 150 points, a 40% bar and
  "Welcome back!" to a seconds-old account and **no test asserted against any of it**. These
  checks make those unable to return quietly. Groups 9 to 14 were added for Cycle 3 and cover the
  narrow-width layout, navigation semantics, the single-filled-control invariant, the bounded
  first action, the failure state, and the skeleton timing. **Group 0 parses every script with
  `node --check`** — added after a 2026-07-30 edit closed an array with a brace, `main.js` failed
  to parse, no handler bound, and "Get started" did nothing while all 80 text-pattern checks stayed
  green. A suite that reads source as text cannot tell working code from a syntax error.

## Status log

| Date | Entry |
|---|---|
| 2026-08-07 | **Prototype cleanup & build update executed.** Verified active multi-file prototype in `src/` against `src/src-prototype.test.ps1` (**PASSED, 465 checks, 25 groups**). Updated `build-standalone.ps1` to target `src/` prototype source and asset paths, and successfully re-compiled `standalone.html`. Removed stale legacy flat files from project root (`prototype-web.html`, root `main.js`, root `data.js`, root `styles.css`, root `img/`, `prototype-web.test.ps1`) to avoid developer confusion. Re-verified build script and test suite after cleanup (**PASSED**). |
| 2026-07-31 | **Cycle 7 / Slice 19 built: the search field, three starters, and a simulated result list.** Three changes at the product owner's direction, two of which overturn Slice 17's own acceptance criteria. **(1) The pill became an `<input>`.** Slice 17 shipped a *button* labelled *"What do you want to learn?"*, so a learner who read the question and started typing produced nothing — the affordance and the copy disagreed, and that is a defect fix rather than a feature. The `<label>` is visually hidden and kept, because a placeholder is not an accessible name. **(2) Three starters**, a declared constant in `data.js`, identical for every learner; activating one **fills the field and does not ask**, so the learner keeps the edit. **(3) A simulated result list.** §14's exclusion of the recommendation surface is struck through with its reasoning reproduced verbatim, exactly as the Slice 17 overturn was. **The service is still excluded** — nothing calls a ranker, an index, or an activity source. What was bought is the right to *simulate the surface*, and it is paid for in labels at the point of use: the list states beside its cards that the catalogue is placeholder content, an unmatched query says so and offers **no substitute set**, and a topic fill-in renders under its own *"Related"* heading and is **never counted as a match**. A new §2 assumption carries the sharp risk — that a learner reads a labelled placeholder as a placeholder — with a falsification condition **on the label, not the feature**. **Slice 17's third prohibition is untouched and is why this was affordable:** asking cannot re-point Up Next, and a result card is inert and says so. Verified in-browser: `money` searched twice appears once in the recent list, a result card left `initialCourseId` at `eng-1` and Up Next unchanged, and `underwater basket weaving` returned no courses at all. **One bug found by the fix and fixed:** Escape closed the panel and left focus in the field, and `focus` cannot fire on an element that never lost it — so the collapse was one-way for a keyboard learner. Typing, clicking, and asking now all re-open it. `src-prototype.test.ps1`: **PASSED, 431 checks, 25 groups.** **Not verified: the 360px composition** — this session's browser would not size below 1280px. The relocation logic is unchanged and its checks pass, but the expanded panel's narrow-width behaviour has not been seen. **Neither Cycle 7 gate has run**, on the same standing exception recorded for Cycles 4 and 5. |
| 2026-07-31 | **The learner's chosen goal becomes the tracker's headline, as a soft yellow chip; "Your current learning path" demoted to a fallback.** At the product owner's direction. **Why the goal earns it:** it is the only thing on this surface the learner supplied in their own words — the course, chapters and counter are all the system's resolution of it — and §2.1 F3 ("the home renders the intake result") was until now satisfied only by implication, through a course title the learner never typed. It passes §7.6's N-of-one rule on its own terms: *what was asked for* and *what it resolved to* are two facts, not one said twice. **The fallback is not caution:** a program learner has no goal, so with only the chip that slot would be unlabelled — reproducing the exact complaint that put a heading there. Exactly one of the two renders, never both, never neither; a missing or unrecognised goal id resolves to `null` and hides the chip rather than printing a raw id. The title comes from the same lookup the intake screen presents from, so a re-worded goal set moves both together. **The chip stays visible at 360px** although the generic label does not: that rule's reason is about the *course*, which the Up Next card already names, and the card never states the goal. **A contrast bug found and fixed by measurement, not by eye:** the label was `--ink2`, which reads 8.02:1 on the light chip but **4.26:1 on the dark one — under AA, on 10px text.** Both lines now sit at full `--ink` with the hierarchy carried by size, weight and case; measured 14.9:1 light and 6.09:1 dark. **No `opacity` on either line**, deliberately — a translucent label lowers real contrast while `getComputedStyle` still reports the full token, so it is a failure no contrast check can see. The brand yellow is used only as a **mixed ground** (`--pri` is #ffcb1d, about 1.7:1 on white — a fill colour, not a text colour). `src-prototype.test.ps1`: **PASSED, 465 checks, 25 groups.** |
| 2026-07-31 | **The Up Next card fills its column, and gained placeholder cover art.** Both at the product owner's direction. **The height was already there** — `.home-main` is `flex: 1` inside a `100dvh` card, and `align-items: start` on the layout grid was sizing both tracks to content, so the dominant object sat short with the viewport empty beneath it. Removing it and adding `flex: 1` lets the content column stretch; the rail is unaffected because its children are fixed-height cards. **The spare height goes to the cover, not to whitespace.** Centring the text cluster was tried first and is worse: it floats a small block in a large box and puts distance between the chapter name and the button that opens it. Growing the cover gives the extra height to the one element with no intrinsic size, which is what a cover is for. **The art is stock photography from Unsplash, and the card says "Placeholder cover" on the image itself** — there is no curriculum for a cover to be *of* while the §15 goal-to-course map is open, so an unlabelled plausible photo would be the same fabrication class as a plausible answer behind the search field. Each URL was loaded and **each image looked at before assignment**, so the subject matches the course rather than being inferred from a photo id. Cover art is keyed by course id to the **same catalogue the search results read**, so the two cannot disagree. **Two costs recorded rather than discovered later:** these are external requests (same footing as the existing font and logo links), and **a published Artifact's CSP will block them** — which is why the tinted-gradient-and-glyph fallback is a real design and is what renders there. **One bug shipped and caught in-browser:** the image started `display: none` and was revealed on load, but also carried `loading="lazy"` — and a browser will not load a lazy image that is not being rendered, so it waited forever for a load that never started. Verified: cover keys correctly across two courses, and an id absent from the catalogue degrades to the glyph with no broken image. `src-prototype.test.ps1`: **PASSED, 445 checks, 25 groups.** |
| 2026-07-31 | **Two copy removals at the product owner's direction, each with its accessibility cost paid rather than absorbed.** The tracker's **"Next" chip** is gone: it restated what the primary action says in full a few pixels away (*"Start: Introduce yourself"*), which is the two-names-for-one-thing defect the chapter list was restructured to remove. SC 1.4.1 survives because the three row states differ by **glyph**, not colour — now asserted as its own check, so a later move to one shared icon fails loudly instead of silently reducing the list to colour alone. `aria-current="step"` is unchanged. The top bar's **"Profile" word** is gone, leaving the avatar glyph: it was competing for horizontal room with the four destination families whose labels do carry weight. **The `aria-label` is the price and is the criterion** — an icon-only control with no accessible name fails SC 4.1.2, and it is what the profile panel's relationship is announced from; asserted across all three views. |
| 2026-07-31 | **"Your courses" is no longer rendered for a single enrolment, and the ambiguous chip label is gone.** Prompted by a second comprehension complaint. **Slice 16's criterion always allowed both arms — *"either shows that one course or is not rendered"* — and this build took the first.** With one enrolment the section was a plural heading over one chip that named the course already titled in the card 40px above it, marked "Open" (an imperative), answering every click with "you are already in it". A control with one option is not a control; it is a duplicate label. Now gated on `length > 1`, the chip reads **"Current"**, and the criterion's either/or is **closed** — an open arm in an acceptance criterion is a decision deferred to whoever writes the code. The condition is on **data, not first run**: a returning learner with one enrolment has nothing to switch between either. Verified: 1 enrolment → not rendered, content column holds only Up Next; 2 enrolments → both chips render, keyboard-reachable, current one carries `aria-current`, neither filled. `src-prototype.test.ps1`: **PASSED, 374 checks, 25 groups.** |
| 2026-07-31 | **New PRD §7.6, "The N-of-one rule" — written because two complaints in a row were the same defect.** The progress bar at `0%` and the switcher at one course were both **steady-state furniture rendered into an empty room**, and neither was a labelling problem, which is why neither would have been fixed by renaming it. **Both also had their counter-argument already in this document** — §11.1's reasons for relocating the counter and sub-levelling the switcher at 360px — each scoped to narrow width when *neither argument was ever about width*. That is the failure mode the rule names: a correct argument filed under the wrong condition. The rule: for each element, name the value it carries when the learner has done nothing; if that value is "what another element already says" or "nothing they do not already know", it is reframed or not rendered. **Two elements are put on notice rather than changed:** the XP counter reads `0 XP` and is the same shape (left alone — it was a deliberate product decision, and reversing it is the owner's call), and the Ask field passes the rule but carries the separate §2 risk already recorded. |
| 2026-07-31 | **"Skill" renamed to "chapter" everywhere, at the product owner's direction: a course contains chapters and nothing else.** Not just copy — the identifiers, the DOM ids, the handoff keys and the §13 data-model field all carry the one word (`chapterTotal`, `chaptersDone`, `courseChapters`, `firstChapterTitle`, `openChapter`, `chapter_screen`). **A data model saying `skill` under a UI saying `chapter` is the same two-names-for-one-thing defect the `COURSE_MAP` restructure had just removed**, so a copy-only rename would have reintroduced it one layer down. `data.js` was deliberately untouched: `life_skills` and "Language skills" are **goal** vocabulary, not the course unit, and a blind rename would have produced "Life chapters". Verified end to end by walking the real funnel — `finishOnboarding()` writes `chapterTotal: 5` and `courseChapters: [...]`, and the home renders them. |
| 2026-07-31 | **"First unit of your course" retired — "unit" was a third word for the same thing, and the line was false.** It said *First* to a learner on chapter 3. Now derived: `Chapter N of M`. |
| 2026-07-31 | **The headline progress value became a percentage at the product owner's direction, and Slice 12's denominator requirement was preserved rather than dropped.** `0%` names no denominator, so read against the count alone the criterion would simply have failed. The denominator moved into the condition line — *"Finish 1 of 5 chapters to see progress here"* — which is the sentence that says what would move the number and is arguably where it always belonged. §9 and the §11.1 affordance row are amended to say which line now satisfies the criterion. **What is not permitted is dropping it**: a bare `0%` over open-ended encouragement is the exact state F2 was adopted to prevent, and the chapter list does not substitute (a learner who counts rows is doing the system's work). **No claim is made that either framing is clearer** — `research/PATTERNS.md`'s 2026-07-29 peer review retracted the comparable claim as "confounded and untested". The bar width and the printed number are now **one computation**, so they cannot disagree. `src-prototype.test.ps1`: **PASSED, 371 checks, 25 groups.** Round trip verified: 0% → 40%, bar and number equal, `Chapter 3 of 5`, action advanced to "Continue: Spot a trend". |
| 2026-07-31 | **Cycle 6 / Slice 18 built: the "Course progress" rail slot becomes a course tracker.** Prompted by learners reporting they did not know what the slot was about. **The diagnosis is a Cycle 4 regression against this PRD's own reasoning** — §11.1 explains the 360px relocation by saying the counter "describes the course the dominant object names, and the counter and its subject should not be separated", and Cycle 4 separated them at wide width anyway: the card read *Course progress · 0 of 5 chapters* and never named which course. The heading is now the course itself, and the slot enumerates that course's chapters with done / current / not-started states. Rows are non-interactive, per the browse-surface anti-pattern in `research/PATTERNS.md`. `src-prototype.test.ps1`: **PASSED, 355 checks, 25 groups.** |
| 2026-07-31 | **`COURSE_MAP` restructured from a count to a list, which is an integrity fix independent of the tracker.** It carried a **count** field alongside a separate **first-item title** field — two fields describing one thing, free to disagree with nothing to notice. `chapters` is now the list; `chapterTotal` is `chapters.length` and `firstChapterTitle` is `chapters[0]`, so a count contradicting its contents is no longer expressible. The handoff carries the list for the same reason it carries the denominator. **The ~40 chapter titles are fixture content** under the same caveat as the teen course titles: the real course structure is a §15 Program Operations input, not a curriculum claim made here. |
| 2026-07-31 | **A shipped defect the tracker exposed rather than caused.** With only `firstChapterTitle` on the handoff, the primary action named `chapters[0]` for every learner while `openChapter()` opened the chapter at their real position — so a learner two chapters in saw *"Continue: Read a bar chart"* on a button that opened chapter 3. **The label and the destination disagreed and nothing on screen could reveal it.** Putting the outline beside the button made it visible in one glance. Both now read the same list; four checks added. Recorded because it is evidence for a claim this project keeps making: a surface that states its structure catches errors a surface that summarises it cannot. |
| 2026-07-31 | **The reframe is a labelled assumption, and the repo blocked the obvious over-claim.** `research/PATTERNS.md` supports naming the subject (2026-07-29 study F6, `Current Lesson: <name>` beside a bar on **3 of 7** platforms) — but the same entry carries a **peer-review retraction**: that one progress framing beats another at low progress is *"confounded and untested"*. So the PRD does **not** claim the tracker is clearer. It claims the slot had no referent, which is checkable, and that enumerating a denominator is a stronger form of Slice 12's criterion than stating it, which is an argument. The prompting feedback is recorded as **informal — no plan, no method, no participant count, not written up in `research/`** — and treated as the first (negative) signal on the §2 two-column assumption, not as evidence for the replacement. Falsification condition stated; validation is the §2 first-click test plus an **open** comprehension question, since a prompted one would lead the answer. |
| 2026-07-31 | **Rail swapped to the left at the product owner's direction; document order unchanged.** Slice 16's criterion permits this in as many words — reading order places the content column before the rail *regardless of which side the rail is painted on* — so this is a placement change and not a criterion change. Done with explicit `grid-column`, not `order` or `direction: rtl`: all three paint identically, but `order` is the mechanism that gets copied onto a flex container where it *does* break reading order, and `rtl` would flip inline alignment inside both columns as a side effect. Measured at 1280px: rail painted at x=254, content at x=598, content still the wider track, no horizontal overflow; at 357px the placement resets to a single track and the surface measures 337px against a 357px viewport. **The cost, recorded rather than discovered later:** painted order and focus order now disagree at ≥768px, so a *sighted keyboard* user tabs into Up Next on the right and then jumps left to the rail. WCAG 2.2 SC 2.4.3 asks for a meaningful sequence rather than a visual one, so this is not a failure, but it is what the swap buys. A screen-reader user is unaffected — the primary action still comes first, which is the thing the criterion protects. `src-prototype.test.ps1`: **PASSED, 314 checks, 24 groups.** |
| 2026-07-31 | **One guard-suite check narrowed, and its message corrected.** The Slice 16 group banned CSS track reordering outright, on a comment claiming any reorder "separates the painted order from the reading order". That was stricter than the criterion it guarded and wrong on the mechanism: `order` and `grid-column` both leave reading order untouched. It now holds the swap to one mechanism instead — `direction: rtl` stays banned for its inline-alignment side effect, `order` on the two columns is rejected in favour of explicit placement, and two new checks assert the placement exists and is reset at 767px. The document-order check is unchanged and is what actually enforces the criterion. |
| 2026-07-31 | **Cycle 5 / Slice 17 built: the Ask entry point.** A *"What do you want to learn?"* field at the head of the rail, expanding in place into a question box with a multi-line input, an inert attachment stub and an outlined submit. It records the question and states that answering is out of scope; **nothing is returned, nothing is suggested, and the primary action is untouched** — verified by isolating `submitAsk()` and asserting it writes none of the five fields Up Next reads. The zero state is the learner's own previous questions and says so when there are none; no seeded example, because a seeded example is a suggestion. At 360px the field is **kept above the dominant object**, the only slot on this surface that outranks the learner's own work, with the reason stated in §11.1 rather than assumed. `src-prototype.test.ps1`: **PASSED, 311 checks, 24 groups** (group 24 added; Slice 16's rail closed-list check re-written to assert an exact three-member set rather than a count, so a fourth slot fails even if the totals happen to match). Verified in a browser: submitting a question left `Start: Read a bar chart · 8 min` unchanged, and one filled control across both columns at both widths. |
| 2026-07-31 | **§14 exclusion overturned at the product owner's direction, and the reasoning kept.** The entry excluding a natural-language Ask field is struck through rather than deleted, with its original argument reproduced verbatim, because that argument is still the argument against the slice. **§2.1 F3 stays Adopted** — the row says the home renders the intake result and it still does, since Up Next resolves from the stored course id ahead of the field in reading order. What is unevidenced is narrower and is recorded as a new §2 labelled assumption, *"The Ask entry point is an assumption, and the one relevant observation points against it"*: that a learner shown this box ninety seconds after answering the goal step does not read it as the product having ignored their answer. The 2026-07-28 study's CodeSignal observation runs the other way. Falsification condition stated; validation is the §2 first-click test plus two added Ask measures. **Three costs are carried, not resolved** — answer quality in Bahasa Indonesia, a latency and cost budget, and a moderation position for 13-to-17-year-olds — and the slice is buildable only because the prototype answers nothing. A shipped version cannot take that route. **Neither Cycle 5 gate has run**, on the same standing exception recorded for Cycle 4. |
| 2026-07-31 | **Cycle 4 / Slice 16 built: the first-run Learning Home is two columns.** A dominant content column carrying exactly one object (Up Next, with its next item named and the one filled control), and a rail carrying the program card and the course-progress counter and nothing else. The counter **relocates** out of the Up Next card and the program card out of the content column; nothing new is rendered, so the rail acquires content without acquiring a progress affordance or a gamification mechanic. A course switcher renders the enrolment below the dominant object as chips, never padded with locked, suggested or popular courses — `enrolledCourses` is now written onto the handoff at finalization, from the course that actually resolved, and reads `[]` when none did. **PRD 11.1's three Cycle 4 dispositions are executed rather than described**, by a `matchMedia` listener at the same 767px the stylesheet uses: at 360px the counter returns to the Up Next card, the program card becomes the dominant object, and the switcher is sub-levelled. Each node is moved, never cloned. `src-prototype.test.ps1`: **PASSED, 267 checks, 23 groups** (groups 21–23 added: the composition and the rail's closed list, the switcher, and Slice 12/13 enumerated against the recomposed surface, which Slice 16's Regression criterion requires rather than assumes). Verified in a real browser at 1440px organic and program, and at 360px in a 357px-wide frame: one filled control across both columns at both widths, no horizontal overflow, zero counters still stating their condition in-slot. |
| 2026-07-31 | **Gate exception, recorded rather than left silent.** `PRD.md` line 4 states Cycle 4 "must not be built until the stakeholder chain has vetted its slice and Mode S has passed the document". It was built first, at the product owner's direction, on the reading that a clickable prototype is the cheapest input to those reviews rather than their output. **Neither gate has run**, and the entry above is not evidence that either would pass. Two consequences carry: the §2 two-column composition is still a **labelled assumption** whose validation path is a first-click test against the block stack it replaces, and §6.3's ordering constraint (Cycle 4 before Cycle 3, because Slice 16 changes the slot set Slice 14's §11.1 table is written against) was **already violated** — Cycle 3 shipped 2026-07-30. The §11.1 table has since been amended for Cycle 4, so the two agree today; what was not paid is the re-scoping §6.3 predicted, and any narrow-width conformance work should be re-read against the new slot set rather than assumed still valid. |
| 2026-07-31 | **One guard-suite check narrowed, not weakened.** Groups 9 and 10 asserted `-notmatch '<aside'`, banning the element rather than the defect; both messages have always named the defect as *navigation inside a complementary landmark*. Slice 16's rail is the one thing `<aside>` is for, so the check now isolates each `<aside>` and fails only if it contains the navigation — and gains a check that every complementary landmark carries an accessible name, which the old blanket ban made unnecessary and the new allowance makes load-bearing. |
| 2026-07-30 | **Learning Home navigation moved from sidebar to top bar, and the IA reorganized.** Production's eleven flat destinations become four top-level families — **Home, Learn, Portfolio, Work** — with Inbox as an icon control and Referral in the profile menu. All eleven stay reachable; **nine move one level down** and maximum depth is two. Adds an XP counter, a day/night switch and a language switch at the product owner's direction. **XP is derived from durable data and reads zero at first run** — a literal there would be the 150-point pill Cycle 2 was commissioned to remove, and the guard suite now fails on any non-zero XP literal. Dark mode is a tonal variant re-pointing surface and text tokens. Resolves PRD §6.2 entry condition 5 and retires the `Achievements` reconciliation. **Measured at 320/360/414/768/1440 on both pages: 0 overflowing elements, 0 targets below 24×24**, four labels on one line each in English and against plausible Bahasa strings; the bar is one row from 820px up and two at 768px, which is left as it is. Five defects were found by measuring rather than reading: two hard-coded colours that would have left dark mode light, a dark hairline at 1.41:1 against the page, a panel border at 1.2:1 on white, family wrappers with no flex rule (Home took 167px of a 360px row), and a bar that clipped its own open panel. Suite **176 → 182 checks, 19 groups**. **The family names and the item assignment are unvalidated** — a judgement from labels; the study declined to recommend any mapping because ten of the eleven destinations were never opened. Gate: content inventory, open card sort in Bahasa (n≈15), first-click test. Spec: `docs/superpowers/specs/2026-07-30-learning-home-top-nav-ia-design.md`; plan: `docs/superpowers/plans/2026-07-30-learning-home-top-nav-ia.md`. |
| 2026-07-21 | Project started; prototype source authored. |
| 2026-07-23 | `PRD.md` written (Shape-Up, 13 sections + 2 appendices). |
| 2026-07-27 | `README.md` backfilled to the `design-projects.md` contract; `landing.png` / `register.png` downscaled 2528px → 1080px. |
| 2026-07-28 | `SPEC.md` deleted — byte-identical to `PRD.md` apart from line endings in the §7.1 Mermaid block. |
| 2026-07-28 | Stays `Design system: independent` — the spec's Phase 3 migration is dropped, not deferred. Known consequence: `prototype-web.html`'s four external font `<link>` tags mean the committed `standalone.html` would fail `check-prototype.ps1` rule 1, so this prototype is not gate-clean and is not expected to be. |
| 2026-07-28 | **Gender gate reconciled into the PRD.** The screen existed in the prototype but in no requirement. Now documented across §2 (as an explicit *assumption*, not a finding — no study backs it), §7.1/§7.2, Slice 5 (renamed "Age and gender segmentation, and eligibility handling") + 6 ACs, §10, §11 (a new open-question row: Program Ops + Legal/Privacy must confirm purpose and lawful basis, **default = do not collect**), §12.5, and both appendices. The step is flagged provisional and behind its own config flag, so a negative decision removes it without touching the rest of the funnel. |
| 2026-07-28 | Prototype defect pass. Fixed: goal→course map was fully stale (zero key overlap with `data.js`, so every organic learner saw the fallback course); account-creation password gate raised 6→8 per PRD §7.3 (login left at 6 so legacy credentials aren't locked out); modals gained `role="dialog"`/`aria-modal`/accessible names, focus trap, inert background, Escape-to-close, and focus restore; `.modal-overlay` gained `visibility: hidden` so closed modals leave the tab order — which also fixed an `autofocus` firing inside a hidden modal on page load; country-search highlight now escapes the query before building a regex; flag images fail closed instead of showing broken icons; vestigial "at least 15 years old" snackbar copy, four Apple/Telegram handlers for absent elements, and a redundant duplicate back-button registration removed. Suite extended 8 → 12 groups; all pass. |
| 2026-07-28 | Deleted six unreferenced `img/stock-*.webp` (~910 KB) — confirmed zero references project-wide. |
| 2026-07-29 | **Prototype split into `src/` and extended through the Learning Home.** `prototype-web.html` copied to `src/onboarding.html` with the `learning_home` screen lifted out; new `src/home.html` + `src/home.js` carry the first-run home, the first learning action, and the return. The funnel now ends by writing a handoff (name, entry path, resolved `initialCourseId`, chapter total) and navigating to `home.html`, so the course is **stored at finalization** rather than recomputed at render (PRD Slice 13). Cycle 2 realized: first-run greeting (no more "Welcome back!" to a seconds-old account), `0 of N chapters` with the countable condition **in the same slot**, no streak pill, no points pill, and an unmapped goal that shows an empty state instead of substituting a course. Completing the first chapter returns to the same markup with real values, which demonstrates the Slice 12 rendering invariant. Verified in-browser over `localhost`: organic, program, unmapped, and returning paths all walk end to end. The four fabrications at `prototype-web.html:537-552` and the `|| 'General Chapters Mastery'` fallback are gone from the `src/` build. The original flat files are left untouched as the reference. |
| 2026-07-29 | **PRD revised via `/draft-prd` against a second study.** Added `research/2026-07-28-post-signup-handoff-first-run-home` (benchmark, peer-reviewed) to `Informed by:`, and a §2.1 findings-coverage table accounting for all **16** findings across both studies (13 Adopted · 2 Deferred · 1 Contradicted). Brought the document to the current 17-section template: new §10 Users & Roles, §11 Screens/IA/Empty States, §12 Modal Reference, §13 Data Model (promoted from Appendix B); former §§10–13 renumbered to §§14–17. Added **Cycle 2** (Slices 12–13, first-run Learning Home) with its own **5–6 iteration** appetite; Cycle 1's 10–12 is unchanged. **Slice count: 12** (Slices 1–10, 12–13; Slice 11 retired into Slice 12 at review). Amended Slices 3, 5, 6, 9, 10. Recorded two departures from cited research: LR-F1 try-first **deferred** with a numeric reopen trigger (5.2 below 55%), LR-F7 the 15+ age gate **contradicted** because the product serves 13–17s. Fixed Appendix A.5, which had been instructing the build to render the fabricated progress the new §5.3 guardrail blocks release for. Stakeholder chain: PM, Tech Lead, Head of Product — **8 Go/Conditional Go, 1 No-Go** (Slice 11, merged not cut). **Principal Designer Mode S: revise**, 12 items, all applied. |
| 2026-07-30 | **PRD revised via `/draft-prd` against a third study, adding responsive/narrow-width scope.** Added `research/2026-07-29-learning-home-layout-and-ia` (benchmark, peer-reviewed) to `Informed by:`; §2.1 now accounts for **25** findings across three studies (**20 Adopted · 4 Deferred · 1 Contradicted**), of which two are adopted as **labelled hypotheses** because their evidence is native iOS against an Android-mobile-web target. Added **Cycle 3** (§6.2, **3–4 iterations**): **Slice 14**, the first-run Learning Home at narrow width, plus a **narrow-width conformance pass** covering the seven pre-authentication screens and four modals. New §7.5 narrow-width shape with its own flowchart, §11.1 narrow-width behaviour and per-slot disposition tables, Appendix **A.6**, metrics **5.8/5.9**, and four §5.3 guardrails. **Slice count: 13** (1–10, 12–14; the proposed Slice 15 was ruled **No-Go as a slice** and reconstituted as the conformance pass, deliberately **not** routed to §14). Migrations that kept the prior appetites honest: three structure-and-ranking criteria moved Slice 12 → Slice 14, and two handoff fields moved Slice 13 → Cycle 1's Slice 8. Stakeholder chain round 2: PM, Tech Lead, Head of Product — **1 Conditional Go, 1 No-Go**. **Principal Designer Mode S: revise**, 9 items, all applied. An **independent verification pass** then re-checked every measured claim against source and **refuted three the review chain had asserted as fact** (inline-style count mis-scoped, country listbox described as in-flow when it is absolutely positioned, and "no browser automation anywhere in the workspace", which is false); all corrected. The guardrail downgrade they supported survives on its capability argument, which never rested on the false premise. |
| 2026-07-30 | **Prototype extended to Cycle 3 via `/design-prototype` (hi-fi, full arc).** Built against the revised PRD, using the 2026-07-29 layout-and-IA study as the UI reference rather than fresh Mobbin queries — that study is the peer-reviewed Mobbin research for this exact question, and re-querying would produce unreviewed observations. **Departure from the command as written, per the settled `Design system: independent` state:** its `ui-library/` steps and the `check-prototype.ps1` gate do not apply; the PRD-traceability, state-coverage, copy-vocabulary, DoD, Mode T and PII steps do. Changes: the Learning Home content column extracted from inline styles into classes (the precondition — a media query cannot reach an inline style); navigation became a `nav` landmark of buttons with `aria-current="page"` and `aria-hidden` icons across all three views; the Up Next card changed from a filled block to a surface so the primary button is the only filled control; narrow-width layout with the rail restacked as an icon-above-label destination row; the primary action now names the specific first item and a bounded duration, omitting the cost when absent **or** null; program learners get assigned tasks as the dominant object; shell prerequisite (`dvh`, scoped `overflow`, entry animation ending on `transform: none`, one shared modal-overlay height rule); conformance pass (flexible code segments, de-inlined responsive footer and header, gender opt-out moved into the option grid at equal weight, `.btn-text` raised to the SC 2.5.8 floor). **Measured in-browser at 320/360/414/768/1440 on both pages: 0 overflowing elements, 0 targets below 24×24.** Suite extended **38 → 80 checks, 8 → 14 groups**; mutation-tested, and one check found to be a false pass (a lazy cross-file regex) was tightened until it failed correctly. Flat reference files untouched and still green. **Principal Designer Mode T: revise**, 4 blocking items — no error state (and an unguarded parse that stranded the learner on a permanent skeleton), the two-number skeleton rule declared but never in force, the brand lockup sitting as product-owned chrome above the learner's next action, and the assigned-task denominator being a client-side constant. **All four fixed and verified by behaviour, not markup**: a 150ms wait now shows no skeleton, a 1200ms wait does, `?fail=1` renders a recoverable failure with the shell intact, and the task count reads from the handoff. Nine previously silent destinations now report themselves. |
| 2026-07-30 | **Regression found and fixed: the funnel was broken by this session's own edit.** Splitting `COURSE_MAP` to add `PROGRAM_TASKS` closed the new array with `};` instead of `];`, so `main.js` raised a `SyntaxError`, **no event handler bound, and "Get started" did nothing** — reported by the user. The whole suite was green throughout, because every one of its 80 checks read source as text. Fixed, and the suite gained **group 0: `node --check` on `main.js`, `home.js`, and `data.js`** (83 checks, 15 groups), mutation-tested by reintroducing the exact error. The check needed a second fix of its own: under this file's `$ErrorActionPreference = 'Stop'`, node's stderr became a terminating error, so the suite **aborted instead of reporting a failure** — relaxed to `Continue` around the node calls with all streams discarded, so the exit code is the signal. Full organic funnel then re-walked in-browser end to end: landing → name → country → age → gender → goal → account wall → `home.html`, **zero JS errors**, handoff written with `initialCourseId`, `firstChapterTitle` and `firstChapterMinutes`, home rendering "0 of 4 chapters", "Finish 1 chapter to see progress here", and one filled control. |
| 2026-07-30 | **Age-conditional goal set, one shared choice card, and the age gate made keyboard-operable.** A 13-to-17 learner is offered English & Communication, Math & Science and Life skills; every other band keeps the six. `data.js` carries a band-keyed map plus a `goalOptionsFor` resolver, `setAgeCategory` becomes the one writer of the band and clears a goal whose option set no longer contains it, and three `COURSE_MAP` rows carry the teen goals through the handoff. `.goal-card` retired into a shared `.choice-card` at the age gate's scale, with `.card-wide` lifting the goal grid off the 520px cap at `styles.css:169` **by specificity, not `!important`** (repeating the `:not()` chain scores (0,4,0) against that rule's (0,3,0)) — the grid rendered about 162px per column before, which does not hold a 48px icon. The age gate's seven `div` controls became buttons with `aria-pressed`, both containers gained a labelled `role="group"`, the band icons are hidden from the accessible name, and every inline style on the screen moved into CSS including the sub-range show/hide, which wrote `display` directly. **The suite did not catch the one defect that mattered.** `renderGoalCards()` resolved the band correctly but ran only at `DOMContentLoaded`, when the band is still null, so the teen path rendered the six adult goals while all 120 text-pattern checks passed; found by browser walk, fixed by rendering on screen entry through `updateHeaders`, and the page-load call removed so one entry point remains. Two review-found regressions from the `div`-to-`button` change were also fixed: the four sub-range chips had no focus ring (`button { outline: none }` at `styles.css:59`, and `.age-subrange-card` had no `:focus-visible` rule) and had lost their font size, weight and colour to user-agent button defaults. **Re-measured at 320/360/414/768/1440 on the age gate with sub-ranges open, the teen goal screen, the six-card adult goal screen, and `home.html`: 0 overflowing elements, 0 targets below 24x24.** Measured through same-origin iframes, because Chrome clamps window width well above 320px. Suite extended **83 → 122 checks, 15 → 17 groups**. Flat reference files untouched and still green. |
